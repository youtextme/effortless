# Prompt OS — effortless workspace
Load skill `prompt-os` (project: `.cursor/skills/prompt-os` or `.agents/skills/prompt-os`; local: `~/.agents/skills/prompt-os`), then Read and obey `.agents/router/PROMPT-ROUTER.md` (Cloud) or `~/.agents/router/PROMPT-ROUTER.md` (local) before any substantive work.
This workspace (effortless parent + effortless-repo) is fully routed through Prompt OS — every prompt, every tool (Cursor local / Cursor Cloud / Devin / opencode).
